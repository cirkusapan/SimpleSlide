//This is going to be the main code, i want to build it in segments so see other files. here you will not find code until later.

#include 

#define 

int prevSpeed
int speedchange

float Speed
float maxSpeed
float acceleration


Void setup
{
  setmaxSpeed = x
  setSpeed = 50
  
}


Void Loop setup
{
  if (digitalRead(upbutton)) == LOW
  {
    If (
  }
}

